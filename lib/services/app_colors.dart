import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF0F4F8); // خلفية موحدة
  static const appBar = Color(0xFF4A4A58); // شريط علوي ثابت
  static const primary = Colors.teal;
  static const secondary = Colors.deepOrange;
}
